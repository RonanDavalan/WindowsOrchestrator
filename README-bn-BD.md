# উইন্ডোজ অর্কেস্ট্রেটর

[🇺🇸 English](README.md) | [🇫🇷 Français](README-fr-FR.md) | [🇩🇪 Deutsch](README-de-DE.md) | [🇪🇸 Español](README-es-ES.md) | [🇮🇳 हिंदी](README-hi-IN.md) | [🇯🇵 日本語](README-ja-JP.md) | [🇷🇺 Русский](README-ru-RU.md) | [🇨🇳 中文](README-zh-CN.md) | [🇸🇦 العربية](README-ar-SA.md) | [🇮🇩 Bahasa Indonesia](README-id-ID.md)

উইন্ডোজ অর্কেস্ট্রেটর হলো একটি স্ক্রিপ্ট সংকলন যা PowerShell (`.ps1`) স্ক্রিপ্ট চালানোর জন্য উইন্ডোজের নির্ধারিত কাজ (Scheduled Tasks) ব্যবহার করে। একটি গ্রাফিক্যাল অ্যাসিস্ট্যান্ট (`firstconfig.ps1`) ব্যবহারকারীকে একটি কনফিগারেশন ফাইল `config.ini` তৈরি করতে সাহায্য করে। প্রধান স্ক্রিপ্টগুলো (`config_systeme.ps1`, `config_utilisateur.ps1`) এই ফাইলটি পড়ে নির্দিষ্ট কিছু কাজ সম্পাদন করে:
*   উইন্ডোজ রেজিস্ট্রি কী পরিবর্তন করা।
*   সিস্টেম কমান্ড (`powercfg`, `shutdown`) চালানো।
*   উইন্ডোজ পরিষেবা পরিচালনা করা (স্টার্টআপের ধরন পরিবর্তন এবং `wuauserv` পরিষেবা বন্ধ করা)।
*   ব্যবহারকারী দ্বারা সংজ্ঞায়িত অ্যাপ্লিকেশন প্রসেস শুরু বা বন্ধ করা।
*   `Invoke-RestMethod` কমান্ডের মাধ্যমে একটি Gotify নোটিফিকেশন সার্ভিসে HTTP POST অনুরোধ পাঠানো।

স্ক্রিপ্টগুলো ব্যবহারকারীর অপারেটিং সিস্টেমের ভাষা শনাক্ত করে এবং `i18n` ডিরেক্টরিতে অবস্থিত `.psd1` ফাইলগুলো থেকে ক্যারেক্টার স্ট্রিং (লগ, গ্রাফিক্যাল ইন্টারফেস এবং নোটিফিকেশনের জন্য) লোড করে।

<p align="center">
  <a href="https://wo.davalan.fr/"><strong>🔗 সম্পূর্ণ বিবরণের জন্য অফিসিয়াল হোমপেজ দেখুন!</strong></a>
</p>

<p align="center">
  <img src="https://img.shields.io/badge/লাইসেন্স-GPLv3-blue.svg" alt="লাইসেন্স">
  <img src="https://img.shields.io/badge/পাওয়ারশেল_সংস্করণ-5.1%2B-blue" alt="পাওয়ারশেল সংস্করণ">
  <img src="https://img.shields.io/badge/অবস্থা-সক্রিয়-brightgreen.svg" alt="অবস্থা">
  <img src="https://img.shields.io/badge/ওএস-Windows_10_|_11-informational" alt="ওএস">
  <img src="https://img.shields.io/badge/সমর্থন-১১_ভাষা-orange.svg" alt="সমর্থন">
  <img src="https://img.shields.io/badge/অবদান-স্বাগতম-brightgreen.svg" alt="অবদান">
</p>

---

## স্ক্রিপ্টের কার্যাবলী

`1_install.bat` স্ক্রিপ্টটি `management\install.ps1` চালায়, যা দুটি প্রধান নির্ধারিত কাজ (Scheduled Tasks) তৈরি করে।
*   প্রথমটি, **`WindowsOrchestrator-SystemStartup`**, উইন্ডোজ চালু হওয়ার সময় `config_systeme.ps1` চালায়।
*   দ্বিতীয়টি, **`WindowsOrchestrator-UserLogon`**, ব্যবহারকারী লগ ইন করার সময় `config_utilisateur.ps1` চালায়।

`config.ini` ফাইলের প্যারামিটার অনুযায়ী, স্ক্রিপ্টগুলো নিম্নলিখিত কাজগুলো সম্পাদন করে:

*   **স্বয়ংক্রিয় লগইন ব্যবস্থাপনা:**
    *   `স্ক্রিপ্টের কাজ:` স্ক্রিপ্টটি `HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon\AutoAdminLogon` রেজিস্ট্রি কী-তে `1` মানটি লেখে।
    *   `ব্যবহারকারীর কাজ:` এই ফাংশনটি কার্যকর করার জন্য, ব্যবহারকারীকে প্রথমে রেজিস্ট্রিতে পাসওয়ার্ড সংরক্ষণ করতে হবে। স্ক্রিপ্ট এই তথ্য পরিচালনা করে না। **Sysinternals AutoLogon** ইউটিলিটি একটি বাহ্যিক টুল যা এই কাজটি করতে পারে।

*   **পাওয়ার সেটিংস পরিবর্তন:**
    *   স্লিপ মোড নিষ্ক্রিয় করতে `powercfg /change standby-timeout-ac 0` এবং `powercfg /change hibernate-timeout-ac 0` কমান্ডগুলো চালায়।
    *   স্ক্রিনের স্লিপ মোড নিষ্ক্রিয় করতে `powercfg /change monitor-timeout-ac 0` কমান্ডটি চালায়।
    *   দ্রুত স্টার্টআপ (Fast Startup) নিষ্ক্রিয় করতে `HiberbootEnabled` রেজিস্ট্রি কী-তে `0` মানটি লেখে।

*   **উইন্ডোজ আপডেট ব্যবস্থাপনা:**
    *   `NoAutoUpdate` এবং `NoAutoRebootWithLoggedOnUsers` রেজিস্ট্রি কী-গুলিতে `1` মানটি লেখে।
    *   উইন্ডোজ `wuauserv` পরিষেবার স্টার্টআপের ধরন `Disabled`-এ পরিবর্তন করে এবং এটিতে `Stop-Service` কমান্ডটি চালায়।

*   **দৈনিক রিস্টার্টের সময়সূচী:**
    *   `WindowsOrchestrator-SystemScheduledReboot` নামে একটি নির্ধারিত কাজ তৈরি করে যা নির্দিষ্ট সময়ে `shutdown.exe /r /f /t 60` চালায়।
    *   `WindowsOrchestrator-SystemPreRebootAction` নামে একটি নির্ধারিত কাজ তৈরি করে যা রিস্টার্টের আগে ব্যবহারকারী দ্বারা সংজ্ঞায়িত একটি কমান্ড চালায়।

*   **কার্যকলাপ লগিং:**
    *   `Logs` ফোল্ডারে অবস্থিত `.txt` ফাইলগুলিতে সময়সহ লাইন লেখে।
    *   একটি `Rotate-LogFile` ফাংশন বিদ্যমান লগ ফাইলগুলোর নাম পরিবর্তন করে এবং আর্কাইভ করে। `config.ini`-তে `MaxSystemLogsToKeep` এবং `MaxUserLogsToKeep` কী দ্বারা সংরক্ষণের জন্য ফাইলের সংখ্যা নির্ধারণ করা হয়।

*   **Gotify নোটিফিকেশন পাঠানো:**
    *   যদি `config.ini`-তে `EnableGotify` কী `true` থাকে, তবে স্ক্রিপ্টগুলো নির্দিষ্ট URL-এ একটি HTTP POST অনুরোধ পাঠায়।
    *   অনুরোধটিতে একটি শিরোনাম এবং একটি বার্তা সহ একটি JSON পেলোড থাকে। বার্তাটি সম্পাদিত কাজ এবং সম্মুখীন হওয়া ত্রুটিগুলোর একটি তালিকা।

## পূর্বশর্ত

- **অপারেটিং সিস্টেম**: উইন্ডোজ 10 বা উইন্ডোজ 11। সোর্স কোডে PowerShell স্ক্রিপ্টের জন্য `#Requires -Version 5.1` নির্দেশিকা রয়েছে।
- **অনুমতি**: `1_install.bat` এবং `2_uninstall.bat` চালানোর সময় ব্যবহারকারীকে বিশেষাধিকার বৃদ্ধির (UAC) অনুরোধ গ্রহণ করতে হবে। এই কাজটি স্ক্রিপ্টগুলোকে নির্ধারিত কাজ তৈরি করতে এবং সিস্টেম স্তরে রেজিস্ট্রি কী পরিবর্তন করার অনুমতি দেওয়ার জন্য প্রয়োজনীয়।
- **স্বয়ংক্রিয় লগইন (Auto-Login)**: যদি ব্যবহারকারী এই বিকল্পটি সক্রিয় করে, তবে তাকে রেজিস্ট্রিতে তার পাসওয়ার্ড সংরক্ষণ করার জন্য **Microsoft Sysinternals AutoLogon**-এর মতো একটি বাহ্যিক টুল ব্যবহার করতে হবে।

## ইনস্টলেশন এবং প্রথম কনফিগারেশন

ব্যবহারকারী **`1_install.bat`** ফাইলটি চালায়।

1.  **কনফিগারেশন (`firstconfig.ps1`)**
    *   `management\firstconfig.ps1` স্ক্রিপ্টটি চলে এবং একটি গ্রাফিক্যাল ইন্টারফেস প্রদর্শন করে।
    *   যদি `config.ini` ফাইলটি বিদ্যমান না থাকে, তবে এটি `management\defaults\default_config.ini` টেমপ্লেট থেকে তৈরি করা হয়।
    *   যদি এটি বিদ্যমান থাকে, স্ক্রিপ্টটি ব্যবহারকারীকে জিজ্ঞাসা করে যে সে এটিকে টেমপ্লেট দিয়ে প্রতিস্থাপন করতে চায় কিনা।
    *   ব্যবহারকারী প্যারামিটারগুলো প্রবেশ করায়। "সংরক্ষণ করুন এবং বন্ধ করুন" এ ক্লিক করলে, স্ক্রিপ্টটি `config.ini`-তে মানগুলো লেখে।

2.  **নির্ধারিত কাজ ইনস্টলেশন (`install.ps1`)**
    *   অ্যাসিস্ট্যান্ট বন্ধ হওয়ার পরে, `1_install.bat` বিশেষাধিকার বৃদ্ধির অনুরোধ করে `management\install.ps1` চালায়।
    *   `install.ps1` স্ক্রিপ্টটি দুটি নির্ধারিত কাজ তৈরি করে:
        *   **`WindowsOrchestrator-SystemStartup`**: `NT AUTHORITY\SYSTEM` অ্যাকাউন্ট দিয়ে উইন্ডোজ চালু হওয়ার সময় `config_systeme.ps1` চালায়।
        *   **`WindowsOrchestrator-UserLogon`**: ইনস্টলেশন শুরু করা ব্যবহারকারীর লগ ইন করার সময় `config_utilisateur.ps1` চালায়।
    *   রিস্টার্টের জন্য অপেক্ষা না করে কনফিগারেশন প্রয়োগ করার জন্য, `install.ps1` প্রক্রিয়াটির শেষে একবার `config_systeme.ps1` এবং তারপর `config_utilisateur.ps1` চালায়।

## ইনস্টলেশন-পরবর্তী ব্যবহার এবং কনফিগারেশন

ইনস্টলেশনের পরে যেকোনো কনফিগারেশন পরিবর্তন `config.ini` ফাইলের মাধ্যমে করা হয়।

### ১. `config.ini` ফাইলের ম্যানুয়াল পরিবর্তন

*   **ব্যবহারকারীর কাজ:** ব্যবহারকারী একটি টেক্সট এডিটর দিয়ে `config.ini` ফাইলটি খোলে এবং পছন্দসই মানগুলো পরিবর্তন করে।
*   **স্ক্রিপ্টের কাজ:**
    *   `[SystemConfig]` বিভাগের পরিবর্তনগুলো `config_systeme.ps1` দ্বারা পড়া হয় এবং **কম্পিউটারের পরবর্তী রিস্টার্টে** প্রয়োগ করা হয়।
    *   `[Process]` বিভাগের পরিবর্তনগুলো `config_utilisateur.ps1` দ্বারা পড়া হয় এবং **ব্যবহারকারীর পরবর্তী লগ ইনে** প্রয়োগ করা হয়।

### ২. গ্রাফিক্যাল অ্যাসিস্ট্যান্টের ব্যবহার

*   **ব্যবহারকারীর কাজ:** ব্যবহারকারী আবার `1_install.bat` চালায়। গ্রাফিক্যাল ইন্টারফেসটি খোলে, যা `config.ini`-এর বর্তমান মানগুলো দিয়ে আগে থেকেই পূরণ করা থাকে। ব্যবহারকারী প্যারামিটারগুলো পরিবর্তন করে এবং "সংরক্ষণ করুন এবং বন্ধ করুন" এ ক্লিক করে।
*   **স্ক্রিপ্টের কাজ:** `firstconfig.ps1` স্ক্রিপ্টটি নতুন মানগুলো `config.ini`-তে লেখে।
*   **ব্যবহারের প্রেক্ষাপট:** অ্যাসিস্ট্যান্ট বন্ধ হওয়ার পরে, কমান্ড প্রম্পটটি নির্ধারিত কাজ ইনস্টলেশনের দিকে এগিয়ে যাওয়ার প্রস্তাব দেয়। ব্যবহারকারী শুধুমাত্র কনফিগারেশন আপডেট করার জন্য এই উইন্ডোটি বন্ধ করতে পারে।

## আনইনস্টলেশন

ব্যবহারকারী **`2_uninstall.bat`** ফাইলটি চালায়। এটি একটি বিশেষাধিকার বৃদ্ধির (UAC) অনুরোধের পরে `management\uninstall.ps1` চালায়।

`uninstall.ps1` স্ক্রিপ্টটি নিম্নলিখিত কাজগুলো সম্পাদন করে:

1.  **স্বয়ংক্রিয় লগইন:** স্ক্রিপ্টটি একটি প্রম্পট প্রদর্শন করে জিজ্ঞাসা করে যে স্বয়ংক্রিয় লগইন নিষ্ক্রিয় করা উচিত কিনা। যদি ব্যবহারকারী `o` (হ্যাঁ) উত্তর দেয়, স্ক্রিপ্টটি `AutoAdminLogon` রেজিস্ট্রি কী-তে `0` মানটি লেখে।
2.  **কিছু সিস্টেম প্যারামিটার পুনরুদ্ধার:**
    *   **আপডেট:** এটি `NoAutoUpdate` রেজিস্ট্রি মান `0`-তে সেট করে এবং `wuauserv` পরিষেবার স্টার্টআপের ধরন `Automatic`-এ কনফিগার করে।
    *   **দ্রুত স্টার্টআপ:** এটি `HiberbootEnabled` রেজিস্ট্রি মান `1`-এ সেট করে।
    *   **OneDrive:** এটি `DisableFileSyncNGSC` রেজিস্ট্রি মানটি মুছে ফেলে।
3.  **নির্ধারিত কাজ মুছে ফেলা:** স্ক্রিপ্টটি `WindowsOrchestrator-SystemStartup`, `WindowsOrchestrator-UserLogon`, `WindowsOrchestrator-SystemScheduledReboot`, এবং `WindowsOrchestrator-SystemPreRebootAction` কাজগুলো খুঁজে বের করে এবং মুছে ফেলে।

### প্যারামিটার পুনরুদ্ধার সম্পর্কে নোট

**আনইনস্টলেশন স্ক্রিপ্টটি পাওয়ার সেটিংস পুনরুদ্ধার করে না** যা `powercfg` কমান্ড দ্বারা পরিবর্তন করা হয়েছিল।
*   **ব্যবহারকারীর জন্য পরিণতি:** যদি মেশিন বা স্ক্রিনের স্লিপ মোড স্ক্রিপ্ট দ্বারা নিষ্ক্রিয় করা হয়ে থাকে, তবে আনইনস্টলেশনের পরেও তা নিষ্ক্রিয় থাকবে।
*   **ব্যবহারকারীর প্রয়োজনীয় কাজ:** স্লিপ মোড পুনরায় সক্রিয় করতে, ব্যবহারকারীকে উইন্ডোজের "পাওয়ার এবং স্লিপ সেটিংস"-এ এই বিকল্পগুলো ম্যানুয়ালি পুনরায় কনফিগার করতে হবে।

আনইনস্টলেশন প্রক্রিয়া **কোনো ফাইল মুছে ফেলে না**। প্রকল্পের ডিরেক্টরি এবং এর বিষয়বস্তু ডিস্কে থেকে যায়।

## প্রকল্পের কাঠামো

```
WindowsOrchestrator/
├── 1_install.bat                # গ্রাফিক্যাল কনফিগারেশন এবং তারপর নির্ধারিত কাজ ইনস্টলেশন চালায়।
├── 2_uninstall.bat              # আনইনস্টলেশন স্ক্রিপ্ট চালায়।
├── Close-App.bat                # PowerShell স্ক্রিপ্ট Close-AppByTitle.ps1 চালায়।
├── Close-AppByTitle.ps1         # একটি উইন্ডো তার শিরোনাম দ্বারা খুঁজে বের করে এবং একটি কীস্ট্রোক ক্রম পাঠায়।
├── config.ini                   # প্রধান স্ক্রিপ্ট দ্বারা পঠিত কনফিগারেশন ফাইল।
├── config_systeme.ps1           # মেশিন সেটিংসের জন্য স্ক্রিপ্ট, স্টার্টআপে চালিত হয়।
├── config_utilisateur.ps1       # প্রসেস ব্যবস্থাপনার জন্য স্ক্রিপ্ট, লগইনে চালিত হয়।
├── Fix-Encoding.ps1             # স্ক্রিপ্ট ফাইলগুলোকে UTF-8 with BOM এনকোডিংয়ে রূপান্তর করার টুল।
├── LaunchApp.bat                # একটি বাহ্যিক অ্যাপ্লিকেশন চালু করার জন্য উদাহরণ ব্যাচ স্ক্রিপ্ট।
├── List-VisibleWindows.ps1      # দৃশ্যমান উইন্ডো এবং তাদের প্রসেস তালিকাভুক্ত করার ইউটিলিটি।
├── i18n/
│   ├── en-US/
│   │   └── strings.psd1         # ইংরেজির জন্য ক্যারেক্টার স্ট্রিং ফাইল।
│   └── ... (অন্যান্য ভাষা)
└── management/
    ├── firstconfig.ps1          # গ্রাফিক্যাল কনফিগারেশন অ্যাসিস্ট্যান্ট প্রদর্শন করে।
    ├── install.ps1              # নির্ধারিত কাজ তৈরি করে এবং স্ক্রিপ্টগুলো একবার চালায়।
    ├── uninstall.ps1            # নির্ধারিত কাজ মুছে ফেলে এবং সিস্টেম সেটিংস পুনরুদ্ধার করে।
    └── defaults/
        └── default_config.ini   # প্রাথমিক config.ini ফাইল তৈরির জন্য টেমপ্লেট।
```

## প্রযুক্তিগত নীতি

*   **নেটিভ কমান্ড**: প্রকল্পটি একচেটিয়াভাবে উইন্ডোজ এবং PowerShell-এর নেটিভ কমান্ড ব্যবহার করে। কোনো বাহ্যিক নির্ভরতা ইনস্টল করার প্রয়োজন নেই।
*   **সিস্টেম লাইব্রেরি**: সিস্টেমের সাথে উন্নত মিথস্ক্রিয়া শুধুমাত্র উইন্ডোজে অন্তর্নির্মিত লাইব্রেরির উপর নির্ভর করে (যেমন: `user32.dll`)।

## মূল ফাইলগুলোর বর্ণনা

### `1_install.bat`
এই ব্যাচ ফাইলটি ইনস্টলেশন প্রক্রিয়ার প্রবেশ বিন্দু। এটি কনফিগারেশনের জন্য `management\firstconfig.ps1` চালায়, তারপর উচ্চতর বিশেষাধিকার সহ `management\install.ps1` চালায়।

### `2_uninstall.bat`
এই ব্যাচ ফাইলটি আনইনস্টলেশন প্রক্রিয়ার প্রবেশ বিন্দু। এটি উচ্চতর বিশেষাধিকার সহ `management\uninstall.ps1` চালায়।

### `config.ini`
এটি কেন্দ্রীয় কনফিগারেশন ফাইল। এতে নির্দেশাবলী (কী এবং মান) রয়েছে যা `config_systeme.ps1` এবং `config_utilisateur.ps1` স্ক্রিপ্ট দ্বারা পড়া হয় কী কাজ করতে হবে তা নির্ধারণ করার জন্য।

### `config_systeme.ps1`
কম্পিউটার চালু হওয়ার সময় একটি নির্ধারিত কাজ দ্বারা চালিত, এই স্ক্রিপ্টটি `config.ini` ফাইলের `[SystemConfig]` বিভাগটি পড়ে। এটি উইন্ডোজ রেজিস্ট্রি পরিবর্তন করে, সিস্টেম কমান্ড (`powercfg`) চালিয়ে, এবং পরিষেবা (`wuauserv`) পরিচালনা করে প্যারামিটারগুলো প্রয়োগ করে।

### `config_utilisateur.ps1`
ব্যবহারকারীর লগ ইন করার সময় একটি নির্ধারিত কাজ দ্বারা চালিত, এই স্ক্রিপ্টটি `config.ini` ফাইলের `[Process]` বিভাগটি পড়ে। এর কাজ হলো লক্ষ্য প্রসেসের যেকোনো বিদ্যমান ইনস্ট্যান্স বন্ধ করা, তারপর প্রদত্ত প্যারামিটার ব্যবহার করে এটি পুনরায় চালু করা।

### `management\firstconfig.ps1`
এই PowerShell স্ক্রিপ্টটি গ্রাফিক্যাল ইন্টারফেস প্রদর্শন করে যা `config.ini` ফাইলে প্যারামিটার পড়া এবং লেখার অনুমতি দেয়।

### `management\install.ps1`
এই স্ক্রিপ্টটিতে `WindowsOrchestrator-SystemStartup` এবং `WindowsOrchestrator-UserLogon` নির্ধারিত কাজ তৈরির যুক্তি রয়েছে।

### `management\uninstall.ps1`
এই স্ক্রিপ্টটিতে নির্ধারিত কাজ মুছে ফেলার এবং সিস্টেম রেজিস্ট্রি কী তাদের ডিফল্ট মানে পুনরুদ্ধার করার যুক্তি রয়েছে।

## নির্ধারিত কাজ দ্বারা ব্যবস্থাপনা

অটোমেশন উইন্ডোজের টাস্ক শিডিউলার (`taskschd.msc`)-এর উপর নির্ভর করে। স্ক্রিপ্ট দ্বারা নিম্নলিখিত কাজগুলো তৈরি করা হয়:

*   **`WindowsOrchestrator-SystemStartup`**: পিসি চালু হওয়ার সময় ট্রিগার হয় এবং `config_systeme.ps1` চালায়।
*   **`WindowsOrchestrator-UserLogon`**: লগ ইন করার সময় ট্রিগার হয় এবং `config_utilisateur.ps1` চালায়।
*   **`WindowsOrchestrator-SystemScheduledReboot`**: `config_systeme.ps1` দ্বারা তৈরি হয় যদি `config.ini`-তে `ScheduledRebootTime` সংজ্ঞায়িত থাকে।
*   **`WindowsOrchestrator-SystemPreRebootAction`**: `config_systeme.ps1` দ্বারা তৈরি হয় যদি `config.ini`-তে `PreRebootActionCommand` সংজ্ঞায়িত থাকে।

**গুরুত্বপূর্ণ**: টাস্ক শিডিউলারের মাধ্যমে এই কাজগুলো ম্যানুয়ালি মুছে ফেললে অটোমেশন বন্ধ হয়ে যায় কিন্তু সিস্টেম প্যারামিটার পুনরুদ্ধার হয় না। একটি সম্পূর্ণ এবং নিয়ন্ত্রিত আনইনস্টলেশনের জন্য ব্যবহারকারীকে অবশ্যই `2_uninstall.bat` ব্যবহার করতে হবে।

## লাইসেন্স এবং অবদান

এই প্রকল্পটি **GPLv3** লাইসেন্সের অধীনে বিতরণ করা হয়েছে। সম্পূর্ণ পাঠ্য `LICENSE` ফাইলে উপলব্ধ।

বাগ রিপোর্ট, উন্নতির জন্য পরামর্শ বা পুল অনুরোধের মতো অবদানকে স্বাগত জানানো হয়।
