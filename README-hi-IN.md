# विंडोज ऑर्केस्ट्रेटर

[🇺🇸 English](README.md) | [🇫🇷 Français](README-fr-FR.md) | [🇩🇪 Deutsch](README-de-DE.md) | [🇪🇸 Español](README-es-ES.md) | [🇯🇵 日本語](README-ja-JP.md) | [🇷🇺 Русский](README-ru-RU.md) | [🇨🇳 中文](README-zh-CN.md) | [🇸🇦 العربية](README-ar-SA.md) | [🇧🇩 বাংলা](README-bn-BD.md) | [🇮🇩 Bahasa Indonesia](README-id-ID.md)

विंडोज ऑर्केस्ट्रेटर स्क्रिप्ट्स का एक सेट है जो PowerShell स्क्रिप्ट्स (`.ps1`) को चलाने के लिए विंडोज टास्क शेड्यूलर का उपयोग करता है। एक ग्राफिकल असिस्टेंट (`firstconfig.ps1`) उपयोगकर्ता को एक कॉन्फ़िगरेशन फ़ाइल `config.ini` बनाने की अनुमति देता है। मुख्य स्क्रिप्ट्स (`config_systeme.ps1`, `config_utilisateur.ps1`) इस फ़ाइल को विशिष्ट क्रियाएं करने के लिए पढ़ती हैं:
*   विंडोज रजिस्ट्री कुंजियों का संशोधन।
*   सिस्टम कमांड का निष्पादन (`powercfg`, `shutdown`)।
*   विंडोज सेवाओं का प्रबंधन (`wuauserv` सेवा के स्टार्टअप प्रकार को बदलना और उसे रोकना)।
*   उपयोगकर्ता द्वारा परिभाषित एप्लिकेशन प्रक्रियाओं को शुरू करना या रोकना।
*   `Invoke-RestMethod` कमांड के माध्यम से Gotify अधिसूचना सेवा में HTTP POST अनुरोध भेजना।

स्क्रिप्ट्स उपयोगकर्ता के ऑपरेटिंग सिस्टम की भाषा का पता लगाती हैं और `i18n` डायरेक्टरी में स्थित `.psd1` फ़ाइलों से कैरेक्टर स्ट्रिंग्स (लॉग, ग्राफिकल इंटरफ़ेस और सूचनाओं के लिए) लोड करती हैं।

<p align="center">
  <a href="https://wo.davalan.fr/"><strong>🔗 पूरी प्रस्तुति के लिए आधिकारिक होमपेज पर जाएँ!</strong></a>
</p>

<p align="center">
  <img src="https://img.shields.io/badge/लाइसेंस-GPLv3-blue.svg" alt="लाइसेंस">
  <img src="https://img.shields.io/badge/पॉवरशेल_संस्करण-5.1%2B-blue" alt="पॉवरशेल संस्करण">
  <img src="https://img.shields.io/badge/स्थिति-कार्यरत-brightgreen.svg" alt="स्थिति">
  <img src="https://img.shields.io/badge/ऑपरेटिंग_सिस्टम-Windows_10_|_11-informational" alt="ऑपरेटिंग सिस्टम">
  <img src="https://img.shields.io/badge/समर्थन-11_भाषाएँ-orange.svg" alt="समर्थन">
  <img src="https://img.shields.io/badge/योगदान-स्वागत_है-brightgreen.svg" alt="योगदान">
</p>

---

## स्क्रिप्ट्स की क्रियाएँ

`1_install.bat` स्क्रिप्ट `management\install.ps1` को निष्पादित करती है, जो दो मुख्य अनुसूचित कार्य बनाती है।
*   पहला, **`WindowsOrchestrator-SystemStartup`**, विंडोज के स्टार्टअप पर `config_systeme.ps1` को निष्पादित करता है।
*   दूसरा, **`WindowsOrchestrator-UserLogon`**, उपयोगकर्ता के लॉगऑन पर `config_utilisateur.ps1` को निष्पादित करता है।

`config.ini` फ़ाइल के मापदंडों के आधार पर, स्क्रिप्ट्स निम्नलिखित क्रियाएं करती हैं:

*   **स्वचालित लॉगिन का प्रबंधन:**
    *   `स्क्रिप्ट की क्रिया:` स्क्रिप्ट रजिस्ट्री कुंजी `HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon\AutoAdminLogon` में मान `1` लिखती है।
    *   `उपयोगकर्ता की क्रिया:` इस फ़ंक्शन को चालू करने के लिए, उपयोगकर्ता को पहले रजिस्ट्री में पासवर्ड सहेजना होगा। स्क्रिप्ट इस जानकारी का प्रबंधन नहीं करती है। **Sysinternals AutoLogon** यूटिलिटी एक बाहरी उपकरण है जो यह क्रिया कर सकता है।

*   **बिजली सेटिंग्स का संशोधन:**
    *   स्लीप मोड को अक्षम करने के लिए `powercfg /change standby-timeout-ac 0` और `powercfg /change hibernate-timeout-ac 0` कमांड निष्पादित करता है।
    *   स्क्रीन स्लीप को अक्षम करने के लिए `powercfg /change monitor-timeout-ac 0` कमांड निष्पादित करता है।
    *   फास्ट स्टार्टअप को अक्षम करने के लिए रजिस्ट्री कुंजी `HiberbootEnabled` में मान `0` लिखता है।

*   **विंडोज अपडेट्स का प्रबंधन:**
    *   रजिस्ट्री कुंजियों `NoAutoUpdate` और `NoAutoRebootWithLoggedOnUsers` में मान `1` लिखता है।
    *   विंडोज सेवा `wuauserv` के स्टार्टअप प्रकार को `Disabled` में बदलता है और उस पर `Stop-Service` कमांड निष्पादित करता है।

*   **दैनिक पुनरारंभ की योजना:**
    *   `WindowsOrchestrator-SystemScheduledReboot` नामक एक अनुसूचित कार्य बनाता है जो निर्धारित समय पर `shutdown.exe /r /f /t 60` निष्पादित करता है।
    *   `WindowsOrchestrator-SystemPreRebootAction` नामक एक अनुसूचित कार्य बनाता है जो पुनरारंभ से पहले उपयोगकर्ता द्वारा परिभाषित कमांड निष्पादित करता है।

*   **क्रियाओं की लॉगिंग:**
    *   `Logs` फ़ोल्डर में स्थित `.txt` फ़ाइलों में टाइमस्टैम्प्ड लाइनें लिखता है।
    *   एक `Rotate-LogFile` फ़ंक्शन मौजूदा लॉग फ़ाइलों का नाम बदलता है और उन्हें संग्रहीत करता है। रखने के लिए फ़ाइलों की संख्या `config.ini` में `MaxSystemLogsToKeep` और `MaxUserLogsToKeep` कुंजियों द्वारा परिभाषित की जाती है।

*   **Gotify सूचनाएं भेजना:**
    *   यदि `config.ini` में `EnableGotify` कुंजी `true` पर सेट है, तो स्क्रिप्ट्स निर्दिष्ट URL पर एक HTTP POST अनुरोध भेजती हैं।
    *   अनुरोध में एक शीर्षक और एक संदेश के साथ एक JSON पेलोड होता है। संदेश की गई क्रियाओं और आई त्रुटियों की एक सूची है।

## पूर्वापेक्षाएँ

- **ऑपरेटिंग सिस्टम**: विंडोज 10 या विंडोज 11। स्रोत कोड में PowerShell स्क्रिप्ट्स के लिए `#Requires -Version 5.1` निर्देश शामिल है।
- **अधिकार**: उपयोगकर्ता को `1_install.bat` और `2_uninstall.bat` चलाते समय विशेषाधिकार वृद्धि अनुरोध (UAC) स्वीकार करना होगा। यह क्रिया स्क्रिप्ट्स को अनुसूचित कार्य बनाने और सिस्टम-स्तरीय रजिस्ट्री कुंजियों को संशोधित करने की अनुमति देने के लिए आवश्यक है।
- **स्वचालित लॉगिन (ऑटो-लॉगिन)**: यदि उपयोगकर्ता इस विकल्प को सक्रिय करता है, तो उसे रजिस्ट्री में अपना पासवर्ड सहेजने के लिए **Microsoft Sysinternals AutoLogon** जैसे बाहरी उपकरण का उपयोग करना होगा।

## स्थापना और पहली कॉन्फ़िगरेशन

उपयोगकर्ता **`1_install.bat`** फ़ाइल चलाता है।

1.  **कॉन्फ़िगरेशन (`firstconfig.ps1`)**
    *   `management\firstconfig.ps1` स्क्रिप्ट चलती है और एक ग्राफिकल इंटरफ़ेस प्रदर्शित करती है।
    *   यदि `config.ini` फ़ाइल मौजूद नहीं है, तो इसे `management\defaults\default_config.ini` टेम्पलेट से बनाया जाता है।
    *   यदि यह मौजूद है, तो स्क्रिप्ट उपयोगकर्ता से पूछती है कि क्या वे इसे टेम्पलेट से बदलना चाहते हैं।
    *   उपयोगकर्ता पैरामीटर दर्ज करता है। "सहेजें और बंद करें" पर क्लिक करने पर, स्क्रिप्ट मानों को `config.ini` में लिखती है।

2.  **कार्यों की स्थापना (`install.ps1`)**
    *   सहायक बंद होने के बाद, `1_install.bat` विशेषाधिकार वृद्धि का अनुरोध करके `management\install.ps1` चलाता है।
    *   `install.ps1` स्क्रिप्ट दो अनुसूचित कार्य बनाती है:
        *   **`WindowsOrchestrator-SystemStartup`**: विंडोज के स्टार्टअप पर `NT AUTHORITY\SYSTEM` खाते के साथ `config_systeme.ps1` चलाता है।
        *   **`WindowsOrchestrator-UserLogon`**: स्थापना शुरू करने वाले उपयोगकर्ता के लॉगऑन पर `config_utilisateur.ps1` चलाता है।
    *   पुनरारंभ की प्रतीक्षा किए बिना कॉन्फ़िगरेशन लागू करने के लिए, `install.ps1` प्रक्रिया के अंत में एक बार `config_systeme.ps1` और फिर `config_utilisateur.ps1` चलाता है।

## स्थापना के बाद उपयोग और कॉन्फ़िगरेशन

स्थापना के बाद कॉन्फ़िगरेशन में कोई भी संशोधन `config.ini` फ़ाइल के माध्यम से किया जाता है।

### 1. `config.ini` फ़ाइल का मैन्युअल संशोधन

*   **उपयोगकर्ता की क्रिया:** उपयोगकर्ता `config.ini` फ़ाइल को एक टेक्स्ट एडिटर के साथ खोलता है और वांछित मानों को संशोधित करता है।
*   **स्क्रिप्ट्स की क्रिया:**
    *   `[SystemConfig]` अनुभाग के संशोधनों को `config_systeme.ps1` द्वारा **कंप्यूटर के अगले पुनरारंभ पर** पढ़ा और लागू किया जाता है।
    *   `[Process]` अनुभाग के संशोधनों को `config_utilisateur.ps1` द्वारा **उपयोगकर्ता के अगले लॉगऑन पर** पढ़ा और लागू किया जाता है।

### 2. ग्राफिकल असिस्टेंट का उपयोग

*   **उपयोगकर्ता की क्रिया:** उपयोगकर्ता फिर से `1_install.bat` चलाता है। ग्राफिकल इंटरफ़ेस खुलता है, जो `config.ini` के वर्तमान मानों से पहले से भरा होता है। उपयोगकर्ता पैरामीटर संशोधित करता है और "सहेजें और बंद करें" पर क्लिक करता है।
*   **स्क्रिप्ट की क्रिया:** `firstconfig.ps1` स्क्रिप्ट नए मानों को `config.ini` में लिखती है।
*   **उपयोग का संदर्भ:** सहायक बंद होने के बाद, कमांड प्रॉम्प्ट कार्यों की स्थापना के लिए आगे बढ़ने का प्रस्ताव देता है। उपयोगकर्ता केवल कॉन्फ़िगरेशन को अपडेट करने के लिए इस विंडो को बंद कर सकता है।

## स्थापना रद्द करना

उपयोगकर्ता **`2_uninstall.bat`** फ़ाइल चलाता है। यह विशेषाधिकार वृद्धि (UAC) के अनुरोध के बाद `management\uninstall.ps1` चलाता है।

`uninstall.ps1` स्क्रिप्ट निम्नलिखित क्रियाएं करती है:

1.  **स्वचालित लॉगिन:** स्क्रिप्ट एक प्रॉम्प्ट प्रदर्शित करती है जिसमें पूछा जाता है कि क्या स्वचालित लॉगिन को अक्षम किया जाना चाहिए। यदि उपयोगकर्ता `o` (हाँ) का उत्तर देता है, तो स्क्रिप्ट रजिस्ट्री कुंजी `AutoAdminLogon` में मान `0` लिखती है।
2.  **कुछ सिस्टम सेटिंग्स की बहाली:**
    *   **अपडेट्स:** यह रजिस्ट्री मान `NoAutoUpdate` को `0` पर सेट करता है और `wuauserv` सेवा के स्टार्टअप प्रकार को `Automatic` पर कॉन्फ़िगर करता है।
    *   **फास्ट स्टार्टअप:** यह रजिस्ट्री मान `HiberbootEnabled` को `1` पर सेट करता है।
    *   **OneDrive:** यह रजिस्ट्री मान `DisableFileSyncNGSC` को हटा देता है।
3.  **अनुसूचित कार्यों को हटाना:** स्क्रिप्ट `WindowsOrchestrator-SystemStartup`, `WindowsOrchestrator-UserLogon`, `WindowsOrchestrator-SystemScheduledReboot`, और `WindowsOrchestrator-SystemPreRebootAction` कार्यों को खोजती और हटाती है।

### सेटिंग्स की बहाली पर ध्यान दें

**स्थापना रद्द करने वाली स्क्रिप्ट बिजली सेटिंग्स को पुनर्स्थापित नहीं करती है** जो `powercfg` कमांड द्वारा संशोधित की गई थीं।
*   **उपयोगकर्ता के लिए परिणाम:** यदि मशीन या स्क्रीन का स्लीप मोड स्क्रिप्ट्स द्वारा अक्षम कर दिया गया था, तो यह स्थापना रद्द करने के बाद भी अक्षम रहेगा।
*   **उपयोगकर्ता द्वारा आवश्यक कार्रवाई:** स्लीप मोड को फिर से सक्रिय करने के लिए, उपयोगकर्ता को इन विकल्पों को विंडोज के "पावर और स्लीप सेटिंग्स" में मैन्युअल रूप से फिर से कॉन्फ़िगर करना होगा।

स्थापना रद्द करने की प्रक्रिया **किसी भी फ़ाइल को नहीं हटाती है**। प्रोजेक्ट डायरेक्टरी और इसकी सामग्री डिस्क पर बनी रहती है।

## प्रोजेक्ट संरचना

```
WindowsOrchestrator/
├── 1_install.bat                # ग्राफिकल कॉन्फ़िगरेशन चलाता है फिर कार्यों की स्थापना करता है।
├── 2_uninstall.bat              # स्थापना रद्द करने वाली स्क्रिप्ट चलाता है।
├── Close-App.bat                # PowerShell स्क्रिप्ट Close-AppByTitle.ps1 चलाता है।
├── Close-AppByTitle.ps1         # स्क्रिप्ट जो एक विंडो को उसके शीर्षक से ढूंढती है और उसे एक कीस्ट्रोक अनुक्रम भेजती है।
├── config.ini                   # मुख्य स्क्रिप्ट्स द्वारा पढ़ी जाने वाली कॉन्फ़िगरेशन फ़ाइल।
├── config_systeme.ps1           # मशीन सेटिंग्स के लिए स्क्रिप्ट, स्टार्टअप पर निष्पादित।
├── config_utilisateur.ps1       # प्रक्रिया प्रबंधन के लिए स्क्रिप्ट, लॉगिन पर निष्पादित।
├── Fix-Encoding.ps1             # स्क्रिप्ट फ़ाइलों को UTF-8 with BOM एन्कोडिंग में बदलने के लिए उपकरण।
├── LaunchApp.bat                # बाहरी एप्लिकेशन लॉन्च करने के लिए उदाहरण बैच स्क्रिप्ट।
├── List-VisibleWindows.ps1      # उपयोगिता जो दृश्यमान विंडो और उनकी प्रक्रियाओं को सूचीबद्ध करती है।
├── i18n/
│   ├── en-US/
│   │   └── strings.psd1         # अंग्रेजी के लिए कैरेक्टर स्ट्रिंग्स फ़ाइल।
│   └── ... (अन्य भाषाएँ)
└── management/
    ├── firstconfig.ps1          # ग्राफिकल कॉन्फ़geration सहायक प्रदर्शित करता है।
    ├── install.ps1              # अनुसूचित कार्य बनाता है और स्क्रिप्ट्स को एक बार चलाता है।
    ├── uninstall.ps1            # कार्यों को हटाता है और सिस्टम सेटिंग्स को पुनर्स्थापित करता है।
    └── defaults/
        └── default_config.ini   # प्रारंभिक config.ini फ़ाइल बनाने के लिए टेम्पलेट।
```

## तकनीकी सिद्धांत

*   **देशी कमांड**: प्रोजेक्ट विशेष रूप से विंडोज और PowerShell के देशी कमांड का उपयोग करता है। कोई बाहरी निर्भरता स्थापित करने की आवश्यकता नहीं है।
*   **सिस्टम लाइब्रेरी**: सिस्टम के साथ उन्नत इंटरैक्शन केवल विंडोज में एकीकृत पुस्तकालयों पर आधारित हैं (जैसे: `user32.dll`)।

## मुख्य फ़ाइलों का विवरण

### `1_install.bat`
यह बैच फ़ाइल स्थापना प्रक्रिया का प्रवेश बिंदु है। यह कॉन्फ़िगरेशन के लिए `management\firstconfig.ps1` चलाता है, फिर उच्च विशेषाधिकारों के साथ `management\install.ps1` चलाता है।

### `2_uninstall.bat`
यह बैच फ़ाइल स्थापना रद्द करने का प्रवेश बिंदु है। यह उच्च विशेषाधिकारों के साथ `management\uninstall.ps1` चलाता है।

### `config.ini`
यह केंद्रीय कॉन्फ़िगरेशन फ़ाइल है। इसमें निर्देश (कुंजी और मान) होते हैं जो `config_systeme.ps1` और `config_utilisateur.ps1` स्क्रिप्ट्स द्वारा पढ़े जाते हैं ताकि यह निर्धारित किया जा सके कि कौन सी क्रियाएं करनी हैं।

### `config_systeme.ps1`
एक अनुसूचित कार्य द्वारा कंप्यूटर के स्टार्टअप पर निष्पादित, यह स्क्रिप्ट `config.ini` फ़ाइल के `[SystemConfig]` अनुभाग को पढ़ती है। यह विंडोज रजिस्ट्री को संशोधित करके, सिस्टम कमांड (`powercfg`) चलाकर, और सेवाओं (`wuauserv`) का प्रबंधन करके सेटिंग्स लागू करती है।

### `config_utilisateur.ps1`
एक अनुसूचित कार्य द्वारा उपयोगकर्ता के लॉगऑन पर निष्पादित, यह स्क्रिप्ट `config.ini` फ़ाइल के `[Process]` अनुभाग को पढ़ती है। इसकी भूमिका लक्ष्य प्रक्रिया के किसी भी मौजूदा उदाहरण को रोकना है, फिर प्रदान किए गए मापदंडों का उपयोग करके इसे फिर से शुरू करना है।

### `management\firstconfig.ps1`
यह PowerShell स्क्रिप्ट ग्राफिकल इंटरफ़ेस प्रदर्शित करती है जो `config.ini` फ़ाइल में पैरामीटर पढ़ने और लिखने की अनुमति देती है।

### `management\install.ps1`
इस स्क्रिप्ट में `WindowsOrchestrator-SystemStartup` और `WindowsOrchestrator-UserLogon` अनुसूचित कार्यों को बनाने का तर्क है।

### `management\uninstall.ps1`
इस स्क्रिप्ट में अनुसूचित कार्यों को हटाने और सिस्टम रजिस्ट्री कुंजियों को उनके डिफ़ॉल्ट मानों पर पुनर्स्थापित करने का तर्क है।

## अनुसूचित कार्यों द्वारा प्रबंधन

स्वचालन विंडोज टास्क शेड्यूलर (`taskschd.msc`) पर आधारित है। निम्नलिखित कार्य स्क्रिप्ट्स द्वारा बनाए जाते हैं:

*   **`WindowsOrchestrator-SystemStartup`**: पीसी के स्टार्टअप पर ट्रिगर होता है और `config_systeme.ps1` चलाता है।
*   **`WindowsOrchestrator-UserLogon`**: लॉगऑन पर ट्रिger होता है और `config_utilisateur.ps1` चलाता है।
*   **`WindowsOrchestrator-SystemScheduledReboot`**: `config_systeme.ps1` द्वारा बनाया गया यदि `config.ini` में `ScheduledRebootTime` परिभाषित है।
*   **`WindowsOrchestrator-SystemPreRebootAction`**: `config_systeme.ps1` द्वारा बनाया गया यदि `config.ini` में `PreRebootActionCommand` परिभाषित है।

**महत्वपूर्ण**: टास्क शेड्यूलर के माध्यम से इन कार्यों को मैन्युअल रूप से हटाने से स्वचालन रुक जाता है लेकिन सिस्टम सेटिंग्स पुनर्स्थापित नहीं होती हैं। उपयोगकर्ता को पूर्ण और नियंत्रित स्थापना रद्द करने के लिए अनिवार्य रूप से `2_uninstall.bat` का उपयोग करना चाहिए।

## लाइसेंस और योगदान

यह प्रोजेक्ट **GPLv3** लाइसेंस के तहत वितरित किया गया है। पूरा पाठ `LICENSE` फ़ाइल में उपलब्ध है।

योगदान, चाहे वे बग रिपोर्ट हों, सुधार के सुझाव हों, या पुल अनुरोध हों, स्वागत योग्य हैं।
