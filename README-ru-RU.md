# Оркестратор Windows

[🇺🇸 English](README.md) | [🇫🇷 Français](README-fr-FR.md) | [🇩🇪 Deutsch](README-de-DE.md) | [🇪🇸 Español](README-es-ES.md) | [🇮🇳 हिंदी](README-hi-IN.md) | [🇯🇵 日本語](README-ja-JP.md) | [🇨🇳 中文](README-zh-CN.md) | [🇸🇦 العربية](README-ar-SA.md) | [🇧🇩 বাংলা](README-bn-BD.md) | [🇮🇩 Bahasa Indonesia](README-id-ID.md)

Оркестратор Windows — это набор скриптов, который использует Планировщик заданий Windows для выполнения скриптов PowerShell (`.ps1`). Графический помощник (`firstconfig.ps1`) позволяет пользователю создать файл конфигурации `config.ini`. Основные скрипты (`config_systeme.ps1`, `config_utilisateur.ps1`) читают этот файл для выполнения определённых действий:
*   Изменение ключей реестра Windows.
*   Выполнение системных команд (`powercfg`, `shutdown`).
*   Управление службами Windows (изменение типа запуска и остановка службы `wuauserv`).
*   Запуск или остановка определённых пользователем прикладных процессов.
*   Отправка HTTP POST-запросов в службу уведомлений Gotify с помощью команды `Invoke-RestMethod`.

Скрипты определяют язык операционной системы пользователя и загружают строки (для логов, графического интерфейса и уведомлений) из файлов `.psd1`, расположенных в каталоге `i18n`.

<p align="center">
  <a href="https://wo.davalan.fr/"><strong>🔗 Посетите официальную домашнюю страницу для полного обзора!</strong></a>
</p>

<p align="center">
  <img src="https://img.shields.io/badge/Лицензия-GPLv3-blue.svg" alt="Лицензия">
  <img src="https://img.shields.io/badge/Версия_PowerShell-5.1%2B-blue" alt="Версия PowerShell">
  <img src="https://img.shields.io/badge/Статус-Работает-brightgreen.svg" alt="Статус">
  <img src="https://img.shields.io/badge/ОС-Windows_10_|_11-informational" alt="ОС">
  <img src="https://img.shields.io/badge/Поддержка-11_Языков-orange.svg" alt="Поддержка">
  <img src="https://img.shields.io/badge/Вклад-Добро_пожаловать-brightgreen.svg" alt="Вклад">
</p>

---

## Действия скриптов

Скрипт `1_install.bat` запускает `management\install.ps1`, который создаёт две основные запланированные задачи.
*   Первая, **`WindowsOrchestrator-SystemStartup`**, выполняет `config_systeme.ps1` при запуске Windows.
*   Вторая, **`WindowsOrchestrator-UserLogon`**, выполняет `config_utilisateur.ps1` при входе пользователя в систему.

В зависимости от параметров в файле `config.ini`, скрипты выполняют следующие действия:

*   **Управление автоматическим входом:**
    *   `Действие скрипта:` Скрипт записывает значение `1` в ключ реестра `HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon\AutoAdminLogon`.
    *   `Действие пользователя:` Чтобы эта функция работала, пользователь должен предварительно сохранить пароль в реестре. Скрипт не управляет этой информацией. Утилита **Sysinternals AutoLogon** — это внешний инструмент, который может выполнить это действие.

*   **Изменение параметров электропитания:**
    *   Выполняет команды `powercfg /change standby-timeout-ac 0` и `powercfg /change hibernate-timeout-ac 0` для отключения спящего режима.
    *   Выполняет команду `powercfg /change monitor-timeout-ac 0` для отключения выключения экрана.
    *   Записывает значение `0` в ключ реестра `HiberbootEnabled` для отключения Быстрого запуска.

*   **Управление обновлениями Windows:**
    *   Записывает значение `1` в ключи реестра `NoAutoUpdate` и `NoAutoRebootWithLoggedOnUsers`.
    *   Изменяет тип запуска службы Windows `wuauserv` на `Disabled` и выполняет команду `Stop-Service` для неё.

*   **Планирование ежедневной перезагрузки:**
    *   Создаёт запланированную задачу с именем `WindowsOrchestrator-SystemScheduledReboot`, которая выполняет `shutdown.exe /r /f /t 60` в установленное время.
    *   Создаёт запланированную задачу с именем `WindowsOrchestrator-SystemPreRebootAction`, которая выполняет команду, определённую пользователем, перед перезагрузкой.

*   **Ведение журнала действий:**
    *   Записывает строки с временными метками в файлы `.txt` в папке `Logs`.
    *   Функция `Rotate-LogFile` переименовывает и архивирует существующие файлы журналов. Количество сохраняемых файлов определяется ключами `MaxSystemLogsToKeep` и `MaxUserLogsToKeep` в `config.ini`.

*   **Отправка уведомлений Gotify:**
    *   Если ключ `EnableGotify` установлен в `true` в `config.ini`, скрипты отправляют HTTP POST-запрос на указанный URL.
    *   Запрос содержит полезную нагрузку JSON с заголовком и сообщением. Сообщение представляет собой список выполненных действий и возникших ошибок.

## Требования

- **Операционная система**: Windows 10 или Windows 11. Исходный код содержит директиву `#Requires -Version 5.1` для скриптов PowerShell.
- **Права**: Пользователь должен принимать запросы на повышение привилегий (UAC) при выполнении `1_install.bat` и `2_uninstall.bat`. Это действие необходимо, чтобы разрешить скриптам создавать запланированные задачи и изменять ключи реестра на системном уровне.
- **Автоматический вход (Auto-Login)**: Если пользователь включает эту опцию, он должен использовать внешний инструмент, такой как **Microsoft Sysinternals AutoLogon**, для сохранения своего пароля в реестре.

## Установка и первоначальная настройка

Пользователь запускает файл **`1_install.bat`**.

1.  **Настройка (`firstconfig.ps1`)**
    *   Скрипт `management\firstconfig.ps1` запускается и отображает графический интерфейс.
    *   Если файл `config.ini` не существует, он создаётся на основе шаблона `management\defaults\default_config.ini`.
    *   Если он существует, скрипт спрашивает пользователя, хочет ли он заменить его шаблоном.
    *   Пользователь вводит параметры. При нажатии на "Сохранить и закрыть" скрипт записывает значения в `config.ini`.

2.  **Установка задач (`install.ps1`)**
    *   После закрытия помощника `1_install.bat` запускает `management\install.ps1`, запрашивая повышение привилегий.
    *   Скрипт `install.ps1` создаёт две запланированные задачи:
        *   **`WindowsOrchestrator-SystemStartup`**: Выполняет `config_systeme.ps1` при запуске Windows с учётной записью `NT AUTHORITY\SYSTEM`.
        *   **`WindowsOrchestrator-UserLogon`**: Выполняет `config_utilisateur.ps1` при входе пользователя, который запустил установку.
    *   Чтобы применить конфигурацию, не дожидаясь перезагрузки, `install.ps1` выполняет `config_systeme.ps1`, а затем `config_utilisateur.ps1` один раз в конце процесса.

## Использование и настройка после установки

Любое изменение конфигурации после установки производится через файл `config.ini`.

### 1. Ручное изменение файла `config.ini`

*   **Действие пользователя:** Пользователь открывает файл `config.ini` в текстовом редакторе и изменяет нужные значения.
*   **Действие скриптов:**
    *   Изменения в разделе `[SystemConfig]` считываются и применяются `config_systeme.ps1` **при следующей перезагрузке компьютера**.
    *   Изменения в разделе `[Process]` считываются и применяются `config_utilisateur.ps1` **при следующем входе пользователя в систему**.

### 2. Использование графического помощника

*   **Действие пользователя:** Пользователь снова запускает `1_install.bat`. Открывается графический интерфейс, предварительно заполненный текущими значениями из `config.ini`. Пользователь изменяет параметры и нажимает "Сохранить и закрыть".
*   **Действие скрипта:** Скрипт `firstconfig.ps1` записывает новые значения в `config.ini`.
*   **Контекст использования:** После закрытия помощника командная строка предлагает продолжить установку задач. Пользователь может закрыть это окно, чтобы обновить только конфигурацию.

## Удаление

Пользователь запускает файл **`2_uninstall.bat`**. Он выполняет `management\uninstall.ps1` после запроса на повышение привилегий (UAC).

Скрипт `uninstall.ps1` выполняет следующие действия:

1.  **Автоматический вход:** Скрипт отображает запрос, спрашивая, следует ли отключить автоматический вход. Если пользователь отвечает `o` (да), скрипт записывает значение `0` в ключ реестра `AutoAdminLogon`.
2.  **Восстановление некоторых системных параметров:**
    *   **Обновления:** Он устанавливает значение реестра `NoAutoUpdate` в `0` и настраивает тип запуска службы `wuauserv` на `Automatic`.
    *   **Быстрый запуск:** Он устанавливает значение реестра `HiberbootEnabled` в `1`.
    *   **OneDrive:** Он удаляет значение реестра `DisableFileSyncNGSC`.
3.  **Удаление запланированных задач:** Скрипт ищет и удаляет задачи `WindowsOrchestrator-SystemStartup`, `WindowsOrchestrator-UserLogon`, `WindowsOrchestrator-SystemScheduledReboot` и `WindowsOrchestrator-SystemPreRebootAction`.

### Примечание о восстановлении параметров

**Скрипт удаления не восстанавливает параметры электропитания**, которые были изменены командой `powercfg`.
*   **Последствия для пользователя:** Если спящий режим компьютера или экрана был отключён скриптами, он останется отключённым и после удаления.
*   **Требуемое действие от пользователя:** Чтобы снова включить спящий режим, пользователь должен вручную перенастроить эти опции в "Параметрах электропитания и спящего режима" Windows.

Процесс удаления **не удаляет никаких файлов**. Каталог проекта и его содержимое остаются на диске.

## Структура проекта

```
WindowsOrchestrator/
├── 1_install.bat                # Запускает графическую настройку, а затем установку задач.
├── 2_uninstall.bat              # Запускает скрипт удаления.
├── Close-App.bat                # Запускает скрипт PowerShell Close-AppByTitle.ps1.
├── Close-AppByTitle.ps1         # Скрипт, который находит окно по его заголовку и отправляет ему последовательность клавиш.
├── config.ini                   # Файл конфигурации, читаемый основными скриптами.
├── config_systeme.ps1           # Скрипт для настроек компьютера, выполняемый при запуске.
├── config_utilisateur.ps1       # Скрипт для управления процессами, выполняемый при входе в систему.
├── Fix-Encoding.ps1             # Инструмент для преобразования файлов скриптов в кодировку UTF-8 with BOM.
├── LaunchApp.bat                # Пример пакетного файла для запуска внешнего приложения.
├── List-VisibleWindows.ps1      # Утилита, которая перечисляет видимые окна и их процессы.
├── i18n/
│   ├── en-US/
│   │   └── strings.psd1         # Файл строк для английского языка.
│   └── ... (другие языки)
└── management/
    ├── firstconfig.ps1          # Отображает графический помощник настройки.
    ├── install.ps1              # Создаёт запланированные задачи и выполняет скрипты один раз.
    ├── uninstall.ps1            # Удаляет задачи и восстанавливает системные параметры.
    └── defaults/
        └── default_config.ini   # Шаблон для создания начального файла config.ini.
```

## Технические принципы

*   **Нативные команды**: Проект использует исключительно нативные команды Windows и PowerShell. Устанавливать внешние зависимости не требуется.
*   **Системные библиотеки**: Продвинутые взаимодействия с системой основаны только на встроенных в Windows библиотеках (например, `user32.dll`).

## Описание ключевых файлов

### `1_install.bat`
Этот пакетный файл является точкой входа для процесса установки. Он запускает `management\firstconfig.ps1` для настройки, а затем `management\install.ps1` с повышенными привилегиями.

### `2_uninstall.bat`
Этот пакетный файл является точкой входа для удаления. Он запускает `management\uninstall.ps1` с повышенными привилегиями.

### `config.ini`
Это центральный файл конфигурации. Он содержит инструкции (ключи и значения), которые считываются скриптами `config_systeme.ps1` и `config_utilisateur.ps1` для определения, какие действия выполнять.

### `config_systeme.ps1`
Выполняемый при запуске компьютера запланированной задачей, этот скрипт читает раздел `[SystemConfig]` файла `config.ini`. Он применяет параметры, изменяя реестр Windows, выполняя системные команды (`powercfg`) и управляя службами (`wuauserv`).

### `config_utilisateur.ps1`
Выполняемый при входе пользователя в систему запланированной задачей, этот скрипт читает раздел `[Process]` файла `config.ini`. Его роль — остановить любой существующий экземпляр целевого процесса, а затем перезапустить его, используя предоставленные параметры.

### `management\firstconfig.ps1`
Этот скрипт PowerShell отображает графический интерфейс, который позволяет читать и записывать параметры в файл `config.ini`.

### `management\install.ps1`
Этот скрипт содержит логику создания запланированных задач `WindowsOrchestrator-SystemStartup` и `WindowsOrchestrator-UserLogon`.

### `management\uninstall.ps1`
Этот скрипт содержит логику удаления запланированных задач и восстановления системных ключей реестра к их значениям по умолчанию.

## Управление через Планировщик заданий

Автоматизация основана на Планировщике заданий Windows (`taskschd.msc`). Скриптами создаются следующие задачи:

*   **`WindowsOrchestrator-SystemStartup`**: Запускается при старте ПК и выполняет `config_systeme.ps1`.
*   **`WindowsOrchestrator-UserLogon`**: Запускается при входе пользователя в систему и выполняет `config_utilisateur.ps1`.
*   **`WindowsOrchestrator-SystemScheduledReboot`**: Создаётся `config_systeme.ps1`, если в `config.ini` определён `ScheduledRebootTime`.
*   **`WindowsOrchestrator-SystemPreRebootAction`**: Создаётся `config_systeme.ps1`, если в `config.ini` определён `PreRebootActionCommand`.

**Важно**: Ручное удаление этих задач через планировщик заданий остановит автоматизацию, но не восстановит системные параметры. Пользователь должен обязательно использовать `2_uninstall.bat` для полного и контролируемого удаления.

## Лицензия и участие в проекте

Этот проект распространяется под лицензией **GPLv3**. Полный текст доступен в файле `LICENSE`.

Вклад в проект, будь то отчёты об ошибках, предложения по улучшению или запросы на слияние, приветствуется.